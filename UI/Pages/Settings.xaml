<Page
    x:Class="UI.Pages.Settings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UI.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel>
            <TextBlock  x:Uid="ChangeLanguage"  Text="Сменить язык"></TextBlock>
            <ComboBox ItemsSource="{Binding Languages}" 
                      SelectedItem="{Binding SelectedLanguage, Mode=TwoWay}" 
                      SelectedValuePath="Value" 
                      DisplayMemberPath="Text" 
                      HorizontalAlignment="Stretch"></ComboBox>

            <Button  x:Uid="ApplyBtn"  Command="{Binding ChangeLocaleCommand}"></Button>
        </StackPanel>
    </Grid>
</Page>
